# Backend TypeScript + Express + Prisma + MySQL.

This project is a backend service that uses modern development best practices with TypeScript, combining Express for the HTTP server, Prisma as the ORM to interact with MySQL in a type-safe way, making it ideal for applications that require robustness, maintainability and productivity in development.
